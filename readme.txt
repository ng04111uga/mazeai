This is a maze solving AI agent. The structure of the maze has been encoded as a graph in the Problems.py module and the the rest of the modules come together to implement the search algorithm. 

	Run Solution.py to generate the solution.